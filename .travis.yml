# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
cache: packages



addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-4
      - g++-4
      - gfortran-4